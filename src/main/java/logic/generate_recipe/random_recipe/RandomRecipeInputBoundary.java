package logic.generate_recipe.random_recipe;

/**
 * An interface defining the usage of the object for a controller.
 */
public interface RandomRecipeInputBoundary {
    /**
     * Executes the use case.
     */
    void execute();
}